# A default java.util.logging configuration - all AppEngine logging is through java.util.logging by default
#
# To use this configuration, copy it into your application's WEB-INF
# folder and add the following to your appengine-web.xml:
# 
# <system-properties>
#   <property name="java.util.logging.config.file" value="WEB-INF/logging.properties"/>
# </system-properties>

# Set the default logging level for all loggers to ALL, WARNING, INFO, ERROR
.level = INFO

# eof

